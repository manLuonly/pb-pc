//配置侧边栏 及兼容移动端
const state = {
    sidebar: {
        opened: true,
        withoutAnimation: false,
    },
};

const mutations = {};

const actions = {};

export default {
    namespaced: true,
    state,
    mutations,
    actions,
};
